# StreamingTTS — Python dependencies
#
# System packages (already installed by Dockerfile):
#   sox libsox-fmt-mp3 ffmpeg espeak-ng portaudio19-dev libltdl7 libmagic1
# ----------------------------------------------------------------------

# PyTorch (CUDA 12.8 wheels) — match the system CUDA driver
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.8.0
torchaudio==2.8.0

# TTS backends (used only inside engines/viettts/ and engines/engtts/)
omnivoice
kokoro>=0.9.4

# Vietnamese text normalization (used only inside text_normalizer/)
# soe-vinorm itself is installed separately in the Dockerfile with --no-deps
# because its huggingface-hub pin (<1.0) conflicts with the transformers 5.x
# stack. Its other transitive deps must be listed here explicitly:
sklearn-crfsuite
onnxruntime
unidecode
num2words

# Voice changer
praat-parselmouth

# Audio I/O
soundfile>=0.12.0
numpy>=1.26,<2.0

# HuggingFace stack
huggingface_hub>=1.5.0,<2.0
transformers>=5.5.0

# Web server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0
python-multipart>=0.0.6
